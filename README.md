# BTU-PY

LinkVerse — link-in-bio app (Python + PyQt6 + SQLite + Flask)

## Run

```bash
pip install -r requirements.txt
python main.py
```

Register in the desktop app, then share your public profile:

```
http://127.0.0.1:5001/u/yourusername
```
